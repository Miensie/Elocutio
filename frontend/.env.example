# Toutes les variables ici sont exposées côté navigateur (préfixe VITE_
# obligatoire pour Vite). Ne JAMAIS y mettre une clé secrète.

VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# URL du backend Fastify déployé sur Render (ou http://localhost:3000 en local)
VITE_API_BASE_URL=http://localhost:3000
